import { Container } from "react-bootstrap";

function Payments() {
    return (
        <Container className="form-bar">
            <b>Payments.</b>
        </Container>
    );
}

export default Payments;